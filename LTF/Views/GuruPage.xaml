<Page
    x:Class="LTF.Views.GuruPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">
    <Grid
        x:Name="ContentArea"
        Margin="{StaticResource MediumLeftRightMargin}">
        <Grid
            Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Toto je bot Testík, opýtaj sa ho čokoľvek!" HorizontalAlignment="Center" Margin="0,36,0,0" FontSize="20" FontWeight="Light"/>
                <WebView x:Name="webbot" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400" Height="600" Grid.Column="0" Margin="0,60,0,0"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
