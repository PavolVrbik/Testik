<Page
    x:Class="LTF.Views.StatisticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    xmlns:tc="using:Telerik.UI.Xaml.Controls.Chart"
    mc:Ignorable="d">

    <Grid
        x:Name="ContentArea"
        Margin="{StaticResource MediumLeftRightMargin}">
        <Grid
            Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="0" Text="Jednotlivé štatistiky sú k dispozícii cez testovací modul" FontSize="26" FontWeight="Light"/>
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="0" Text="Prejdite v menu na Precvičovanie -> vyberte test -> kliknite na Zobraziť štatistiky" Margin="0,60,0,0" FontSize="20" FontWeight="Light"/>
                <Button Content="Prejsť na stránku precvičovanie" HorizontalAlignment="Center" Margin="0,160,0,0" Click="Button_Click"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
